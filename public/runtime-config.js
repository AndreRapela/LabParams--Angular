// Sobrescritas públicas opcionais. Vazio mantém o ambiente do build: localhost
// no `ng serve` e os padrões de produção no build publicado. Este arquivo pode
// ser substituído depois do build; nunca inclua Secret/Service Role aqui.
window.__SYSMLAB_CONFIG__ = Object.freeze({});
